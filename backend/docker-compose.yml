services:
  postgres:
    image: postgres:16
    container_name: vis-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: vis
      POSTGRES_USER: vis
      POSTGRES_PASSWORD: vis
    ports:
      - "5432:5432"
    volumes:
      - vis-pg:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U vis -d vis"]
      interval: 5s
      timeout: 3s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: vis-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - vis-redis:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  vis-pg:
  vis-redis:
